import { OverdueDirective } from './overdue.directive';

describe('OverdueDirective', () => {
  it('should create an instance', () => {
    const directive = new OverdueDirective();
    expect(directive).toBeTruthy();
  });
});
